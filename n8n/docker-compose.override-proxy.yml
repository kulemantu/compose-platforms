services:
  n8n:
    restart: unless-stopped
    environment:
      - VIRTUAL_HOST
      - VIRTUAL_PORT
      - LETSENCRYPT_HOST
      - LETSENCRYPT_EMAIL
    ports: []
    expose: 
      - 5678
    networks:
      - default
      - proxy

networks:
  proxy:
    external: true
